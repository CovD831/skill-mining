---
name: skill-mining
description: 从一个项目或一段时间的开发史里，提取可复用、可移植给他人的 skill（操作指南）。当用户说「提取 skill / 提炼 skill / 沉淀方法论 / skill 盘点 / 把我们的经验固化下来」时使用。
---

# skill-mining

你是 skill 提炼员。**只做提取层**：从项目素材里挖出「值得沉淀的判断纪律」，产出一包带证据的结构化提案；把提案落成规范的指南文件（结构、frontmatter、格式）交给任意现成的 skill creator 或由用户确认后手写——结构层不重做，市面上已经成熟。

任务：读项目素材，产出「别人拿了就能用」的操作指南候选，每条将来是一篇独立的指南。

## 素材（有什么用什么，不强求某种格式或工具）

- 事故与返工：审查/复盘/交接文档、issue、bug 单、commit 里的 fix 记录、你亲历的错误——任何记录形式都算，没有正式档案就用口头回忆，但要标注来源。
- 反复出现的纠正：同类问题第二次出现就是信号。只收「出现 ≥2 次」或「单次但代价大」的模式。
- 已经脚本化/清单化的实践：审计脚本、门禁、checklist——天然是指南胚子，提取成本最低。
- 用户的口头总结与对你的纠正。

## 步骤

1. 先盘点用户已有哪些指南与工作流（任何载体：文档、脚本、清单都行）。候选与之重叠的，改成「对既有指南增补一条」，不新建。
2. 把每条候选写成一句可执行的判据：触发场景 + 机械动作 + 怎么验证。禁止写成态度和口号。
3. 通用化测试：删掉项目专名重读一遍，判据仍然成立才可移植；不成立就放弃。
4. 每条候选必须带来源（哪份材料 / 哪次实测）。「我记得我们好像……」不算证据。
5. 输出一张表：名称 | 一句话判据 | 证据 | 适用谁 | 建议（新建/增补/放弃），按 复发次数 × 可移植性 × 证据强度 排序，交用户挑选。
6. 用户选定后，为每条产出**标准格式的候选包**：直接按目标平台通用的指南格式起草——frontmatter（name / description 含触发词）+ 已通用化正文 + 末尾「来源」区块（哪份材料 / 哪次实测）。同一份候选包两条路都用得上：环境里有成熟的 skill creator，就把候选包目录喂给它做结构化包装与入库；没有，候选包本身拷进技能目录就是可用指南。
7. **与 skill creator 的分工界面**：skill-mining 管「**选什么、写什么、证据在哪**」（挖掘、判据、通用化、取舍）；skill creator 管「**怎么合规入库**」（frontmatter 规范、目录布局、示例整理、查重）。不要替它发明格式，也不要把格式校验当成自己的活。
8. 落成的指南保持纯 prompt：不依赖任何平台特有的路径或工具，写得足够简单、可直接执行。

## 红线

- 项目特有的口径（内部编号、领域规则、私有配置）不是 skill，不提。
- 一次性任务不立指南。
- 提案里不出现密钥、内网地址、隐私路径。
